# Read all RTL files
read_verilog rtl/*.v

# Analyze hierarchy
hierarchy -check -top TOP

# Synthesize for Xilinx 7-series FPGAs (Standard for LUT area calculation)
synth_xilinx -top TOP

# Show the area report
stat
